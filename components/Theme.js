import { DefaultTheme } from "react-native-paper";

export const Theme = {
  ...DefaultTheme,
  roundness: 25,
  colors: {
    ...DefaultTheme.colors,
    primary: "#4fa7c9",
    accent: "#fff",
    surface: "red",
    background: "#f5fcff",
  },
};
